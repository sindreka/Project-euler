def isPrime(num,li):
	for element in li:
		if num%element or element**(0.5)>num:
			return
	li.append(num)


def main(num):
	li = [2]
	candidate = 3
	while len(li) <= num:
		isPrime(candidate,li)
		candidate +=2
print(main(101)[-1])
